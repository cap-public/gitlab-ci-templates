include:
  - project: 'cap-public/ci/gitlab-ci-templates'
    file: '/elixir/mix.yml'

mix:
  services:
    - postgres:latest
  before_script:
    - mix deps.get --only test
    - mix ecto.create
    - mix ecto.migrate
